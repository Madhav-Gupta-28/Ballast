# dependencies
node_modules/
.pnpm-store/

# foundry
out/
cache/
broadcast/*/dry-run/

# env & keys
.env
.env.*
!.env.example
*.key

# build output
dist/
.next/
coverage/
lcov.info

# os / editor
.DS_Store
*.swp
.idea/
.vscode/

# foundry deps (installed via forge install)
# Anchored to the repo root on purpose: a bare `lib/` also matches web/lib,
# which silently kept the web app's own source out of the repo.
/lib/

# next
.next/
next-env.d.ts

# next build info
*.tsbuildinfo
.vercel
